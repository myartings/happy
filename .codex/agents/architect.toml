name = "architect"
description = "Review system boundaries, interfaces, dependencies, and architectural risk without implementing changes."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
developer_instructions = "Work read-only. Cite repository evidence, distinguish facts from recommendations, and return decisions or blocking questions to the parent session."
