# Flat Session Runtime Status Label

The default flat session list must show a localized runtime state for every row: Running, Idle, Permission required, or last seen/offline. Idle text uses the secondary text color while the existing green dot continues to indicate a connected session.
