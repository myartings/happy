# Restore Grouped Session List Default

Happy Desktop must use the established grouped compact session list by default. Existing installations that inherited the short-lived flat-list default must migrate back once. After migration, users may still explicitly enable the experimental flat list in Settings.
