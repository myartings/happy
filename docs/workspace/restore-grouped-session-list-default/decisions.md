# Decisions: `restore-grouped-session-list-default`

| ID | Question | Status | Decision/evidence |
| --- | --- | --- | --- |
| D1 | How should existing clients leave the newly persisted flat-list default? | resolved | A one-time local-settings migration forces grouped layout once; after the marker is stored, the user can still opt into the flat list. |
