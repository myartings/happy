# Context: `windows-native-reliability`

`context.md` is the human-readable overview. The machine-readable, role-scoped
manifests are:

- `contexts/implement.jsonl`
- `contexts/check.jsonl`

Each JSONL entry contains a repository-relative `path` and a non-empty `reason`.
Keep each role limited to the files it actually needs.

## Implementation context

- `devtools/happyctl.ps1` and its Windows-native smoke seam.
- `packages/happy-wire/package.json` and the Windows-relevant CLI sandbox,
  path, and tool-launcher seams only after their failures reproduce on this
  host.
- No `packages/happy-app` UI, Studio, theme, animation, or visual files.

## Verification context

- Windows PowerShell 5.1 is the required host; installed PowerShell 7 is an
  additional signal.
- Real build commands use an explicit temporary config anchored to this
  worktree because the user's default config intentionally points at the main
  checkout.
- External-state snapshots cover installed executable hashes, uninstall keys,
  Happy tasks, daemon/app process state, Git hook identity, and repository
  status.

## Notes

- The original native implementation began from validated `dev` commit
  `583a37006479b8b86f557b3868eea99086afd224` and remains recoverable on local
  branch `quiet-cloud` at `83f2fd6655951d6725c7bbb6d456991da9ef11ae`.
- The publication candidate was reconstructed without history rewriting on
  `feature/windows-native-reliability` and then fast-forwarded to local and
  `origin/dev` commit `cbf63a29bd3f03767baf5a8e6bd893ecf393ad6a`
  after prerequisite PR `#68` landed. Only the already accepted engineering
  slice and its evidence were restored; current machine state is generated by
  schema 3.
- The main implementation session owns this single coherent slice; no tracker
  Issue, delegated writer, or parallel integration is required. The user has
  explicitly authorized one atomic commit, push, and PR targeting `dev`.
