# Specification Links: `cross-device-active-day-grouping`

- PRD: not required beyond the bug contract; no product outcome or protocol changes
- Feature spec: `docs/specs/cross-device-active-day-grouping.md`
- Architecture: existing client projection boundary documented in `CONTEXT.md`; no repository architecture document exists
- ADRs: none; the existing canonical activity projection is reused
