# Validation: `studio-tool-output-disclosure`

Record exact commands and results. Never mark a check passed unless it ran.
Use `unavailable` when the configured tool is not installed or cannot execute,
and state the consequence under Remaining gaps; unavailable is not a pass.

| Date | Command | Result | Notes |
| --- | --- | --- | --- |
| `2026-08-23` | `git branch --show-current; git status --short; git worktree list` | passed | Started on `dev`; dirty state contained only this feature's spec/task/workflow docs. Created local `feature/studio-tool-output-disclosure` from `dev` without commit or push. |
| `2026-08-23` | inspect `docs/workflow/intensity-matrix.md`, `.ai/project.json`, active workflow links, accepted decisions, and task AC map | passed | Feature intensity confirmed; accepted spec/tasks/decisions cover AC1-AC13; protected/generated paths and configured risk triggers are excluded. |
| `2026-08-23` | trace message detail route → `ToolFullView` → full-view registry and Bash/CodexBash views | passed with scoped refinement | Existing detail route is the full-transcript host. `BashViewFull` preserves output; current `CodexBash` full mapping uses `CodexBashView`, which omits successful enriched output and is assigned to T3. |
| `2026-08-23` | `rg` repository and installed RN/RN-Web sources for `onTextLayout`, `numberOfLines`, `WebkitLineClamp`, layout, and max-height seams | passed | No local measurement abstraction exists. Installed RN types expose text layout; installed RN Web maps `numberOfLines` to visual CSS line clamp. T1/T2 own the feature-local budget seam. |
| `2026-08-23` | trace `ChatList` group state, scroll metrics, and `maintainVisibleContentPosition`; search existing tests | passed with packaged evidence requirement | Inverted-list anchoring and bottom threshold exist, but no mounted ChatList scroll test exists. T5 uses pure policy plus wiring tests; real anchoring remains packaged AC9/AC13 evidence. |
| `2026-08-23` | G0 scoping assessment | ready | Single main-session Feature slice on a branch from `dev`; local-only; no tracker, batch plan, delegated writer, separate worktree, or formal risk gate required. TDD selected for T1-T5. |
| `2026-08-23` | record `risk=not_required`, `scoping=passed`; `workflow-state.py ready ... implementation` | passed | Durable receipts cite decisions D1-D14, exact role manifests, excluded risk triggers, and this validation ledger. |
| `2026-08-23` | `python scripts/workflow-state.py validate studio-tool-output-disclosure`; `python scripts/workflow-audit.py --strict --require-active`; `git diff --check` | passed with expected future gates | Workflow is valid and ready; only implementation/check/review/finish remain pending. Diff integrity passed; existing CRLF conversion warnings are informational. |
| `2026-08-23` | T1 repository-local TDD cycles for successful completion, running tail, failure head/tail, mounted manual override, structured cancellation/interruption/pending state, sanitized copy text, and legacy command/output-only inputs | passed | Every new public behavior produced a meaningful focused RED before the minimum GREEN implementation; no renderer or host wiring changed. |
| `2026-08-23` | `pnpm --filter happy-app exec vitest run sources/features/studio-tool-output-disclosure sources/features/studio-execution-transcript/studioExecutionTranscript.test.ts --testTimeout=15000` | passed | 2 files, 17 tests. Covers the 5-line running tail, 2+8 failure selection, visual-line input seam, manual override, structural status, ANSI/control/CR/truncation, heredoc, long line, narrow wrapping fixture, CJK, emoji, combining, bidi, tabs, empty output, and complete copy separation. |
| `2026-08-23` | `pnpm --filter happy-app typecheck` | passed | Happy App TypeScript check completed with no errors after T1. |
| `2026-08-23` | `python scripts/workflow-state.py validate studio-tool-output-disclosure`; `python scripts/workflow-audit.py --strict --require-active`; `git diff --check` | passed with expected future gates | T1 handoff state is valid and strict audit reports only the still-pending whole-feature implementation/check/review/finish gates. Diff integrity passed; CRLF conversion warnings are informational. |
| `2026-08-24` | T2 repository-local mounted TDD cycles in `ToolViewStudioPresentation.test.ts` | passed | Meaningful REDs proved the prior eager success body, missing summary interaction, missing running preview, missing expansion, boxed compact transcript, and missing wrapped-line measurement seam. Minimum GREEN slices added summary/collapse, bounded previews, manual override, compact Studio wiring, and wrapped visual-line clipping. |
| `2026-08-24` | `pnpm --filter happy-app exec vitest run sources/components/tools/ToolViewStudioPresentation.test.ts sources/features/studio-tool-output-disclosure sources/features/studio-execution-transcript --testTimeout=15000` | passed | 3 files, 42 tests. Mounted coverage proves successful-body absence, one-line metadata, empty output, running 5-line and failure 2+8 bounds, long wrapped lines, 40%-or-480px expansion, manual state precedence, compact on/off, Default fallback, and structured Codex patch preservation. |
| `2026-08-24` | `pnpm --filter happy-app typecheck` | passed | Happy App TypeScript check completed with no errors after T2. |
| `2026-08-24` | `git diff --check` | passed | No whitespace errors; existing LF-to-CRLF conversion warnings are informational. |
| `2026-08-24` | T5 mounted RED for expanded-output follow ownership | passed | The valid RED failed because expanded output had no content-size or scroll handlers. GREEN adds a 24px end threshold: content follows only while the internal viewport was at its end and stops following after the user moves upward. |
| `2026-08-24` | mounted ChatList scroll-anchor wiring test | passed | The inverted FlatList retains `minIndexForVisible: 1` and native 50px bottom-follow threshold. While reading at offset 400, group expansion, streaming data replacement, layout, and content-size changes issue no JS `scrollToOffset` or `scrollToIndex`. |
| `2026-08-24` | mounted streaming and resize behavior tests | passed | A 2,000-line running update keeps the same 95px preview component; viewport height changes recompute the 40% expanded cap from 360px to 240px while preserving the same summary control and manual expanded state. |
| `2026-08-24` | `pnpm --filter happy-app exec vitest run sources/components/ChatListStudioScroll.test.ts sources/components/ToolGroupViewStudioPresentation.test.ts sources/components/tools/ToolViewStudioPresentation.test.ts sources/hooks/useGroupedMessages.test.ts sources/features/studio-tool-output-disclosure --testTimeout=15000` | passed | 6 files, 64 tests. Covers outer native anchoring, internal follow ownership, bounded streaming, resize, mounted identity, Studio-only running-group auto-expansion, completed-group auto-collapse, manual group intent, Default isolation, groups, and disclosure regressions. |
| `2026-08-24` | `pnpm --filter happy-app typecheck` | passed | Happy App TypeScript check completed with no errors after T5. |
| `2026-08-24` | `git diff --check` | passed | No whitespace errors; existing LF-to-CRLF conversion warnings are informational. |
| `2026-08-24` | T3 mounted TDD cycles for output copy, command copy, full-transcript navigation/detail, disclosure semantics, quiet previews, and focus continuity | passed | Focused REDs proved each missing behavior before minimum GREEN changes. Copy actions use complete sanitized model text, the existing message-detail route is reachable from collapsed/preview/expanded states, Studio detail bypasses the incomplete CodexBash specialized view, and accessibility state follows mounted disclosure state. |
| `2026-08-24` | diagnose invalid initial `ToolFullViewStudioTranscript.test.ts` collection failures | passed | Initial collection failures from Expo `__DEV__`, `EventEmitter`, and device imports were not counted as RED. Import tracing identified `@/text` → `expo-localization`; the final host test isolates localization, layout, and the unrelated specialized registry before producing the valid missing-transcript RED. |
| `2026-08-24` | inspect local `node_modules/react-native-web/src/modules/usePressEvents/PressResponder.js` and `src/exports/Pressable/index.js` | passed | Installed RN Web `Pressable` makes controls focusable and treats Enter, Space, and legacy Spacebar as valid presses for button roles; Space prevents page scrolling. This is deterministic keyboard-contract evidence; real packaged keyboard interaction remains T6. |
| `2026-08-24` | `pnpm --filter happy-app exec vitest run sources/features/studio-tool-output-disclosure sources/components/tools/ToolViewStudioPresentation.test.ts sources/components/tools/ToolFullViewStudioTranscript.test.ts sources/features/studio-execution-transcript --testTimeout=15000` | passed | 5 files, 50 tests. Covers complete sanitized copy, omission independence, full-route reachability in all disclosure states, complete Codex success detail, role/name/expanded state, hidden measurement exclusion, quiet streaming previews, mounted focus continuity, and all T1/T2 disclosure regressions. |
| `2026-08-24` | `pnpm --filter happy-app typecheck` | passed | Happy App TypeScript check completed with no errors after T3. |
| `2026-08-24` | T4 mounted TDD cycles for child disclosure routing, group metadata, and mounted child-state retention | passed | Meaningful REDs proved that terminal children previously bypassed disclosure views, group summaries omitted duration/failure count, and collapsing unmounted child disclosure state. Minimum GREEN changes are Studio-terminal scoped. |
| `2026-08-24` | inspect `ChatList.tsx` and nested group initialization/pending effects | passed | Existing group state collapses every non-permission group by default, auto-opens pending-permission groups unless manually collapsed, and retains explicit group choices; no T4 ChatList edit was required. |
| `2026-08-24` | `pnpm --filter happy-app exec vitest run sources/components/ToolGroupViewStudioPresentation.test.ts sources/hooks/useGroupedMessages.test.ts sources/features/studio-tool-output-disclosure --testTimeout=15000` | passed | 4 files, 26 tests. Covers child summary-only disclosure, completed/running/failed/pending composition, group duration/failure metadata, mounted child identity across group collapse, grouping permission metadata, and T1-T3 disclosure regressions. |
| `2026-08-24` | `pnpm --filter happy-app typecheck` | passed | Happy App TypeScript check completed with no errors after T4. |
| `2026-08-24` | `git diff --check` | passed | No whitespace errors; existing LF-to-CRLF conversion warnings are informational. |
| 2026-08-24 | `pnpm --filter happy-app typecheck` | passed | typecheck |
| 2026-08-24 | `pnpm --filter happy-server typecheck` | passed | typecheck |
| 2026-08-24 | `pnpm --filter happy-app exec vitest run` | failed (1) | test |
| 2026-08-24 | `pnpm --filter happy-server test` | failed (1) | test |
| 2026-08-24 | `python3 scripts/validate-happy-workflow.py` | passed | check |
| 2026-08-24 | `python3 scripts/test-workflow-core.py` | passed | check |
| 2026-08-24 | `python3 scripts/test-workflow-ci.py` | failed (120) | check |
| 2026-08-24 | `python3 scripts/workflow-audit.py --strict` | failed (120) | check |
| `2026-08-24` | T6 whole-diff review and focused RED/GREEN correction | passed | Review found that running groups still inherited the old non-permission collapse default. Added mounted REDs, then limited active auto-expansion to Studio, retained explicit manual choices across state changes, auto-collapsed untouched completed groups, and prevented stale running children from opening inside a completed AgentWork group. Default behavior remains unchanged. No unresolved blocking/high/medium finding remains. |
| `2026-08-24` | `pnpm --filter happy-app exec vitest run sources/components/ChatListStudioScroll.test.ts sources/components/ToolGroupViewStudioPresentation.test.ts sources/components/tools/ToolViewStudioPresentation.test.ts sources/hooks/useGroupedMessages.test.ts sources/features/studio-tool-output-disclosure --testTimeout=15000`; `pnpm --filter happy-app typecheck`; `git diff --check` | passed | Final post-review focused verification: 6 files, 64 tests; App typecheck and diff integrity passed. |
| `2026-08-24` | `pnpm --filter happy-app exec vitest run --testTimeout=15000` | passed with unrelated baseline gap | 471/473 files and 1400/1401 tests passed. The only failure is the clean, unchanged `studioSidebarWiring.test.ts` LF-only source substring assertion against CRLF Windows checkout content; the asserted production wiring exists and no Studio-sidebar file is in this feature diff. |
| `2026-08-24` | `pnpm --filter happy-server test` | passed with unrelated baseline gap | 101/102 tests passed. The only failure is unchanged `attachmentRoutes.spec.ts` local-mode encrypted-blob GET expecting 200 and receiving 404. This feature changes no server, route, protocol, storage, or attachment file. |
| `2026-08-24` | `pnpm --filter happy-server typecheck`; `python scripts/test-workflow-ci.py`; `python scripts/workflow-audit.py --strict --require-active studio-tool-output-disclosure` | passed | Server typecheck passed; workflow CI regression suite passed 14 tests; strict active audit passed with only the intentionally pending human/check/finish receipts. The earlier 120 results were parent-command timeout artifacts and passed when run directly. |
| `2026-08-24` | Windows Tauri preflight and `pnpm --filter happy-app tauri:build:production` | passed with packaging warning | Node 20.20.2, pnpm 10.11.0, rustc/cargo 1.95.0, Tauri CLI 2.9.6, and VS 2022 C++ toolchain available. Fresh Studio build produced `Happy_0.1.0_x64_en-US.msi` and `Happy_0.1.0_x64-setup.exe`; both are unsigned. Tauri warned that `__TAURI_BUNDLE_TYPE` could not be patched for updater metadata, so this evidence does not claim updater readiness. |
| `2026-08-24` | `pnpm --filter happy-app tauri:build:dev` and artifact inspection | passed | Fresh logged-in Studio-dev package produced MSI SHA-256 `400F2AB2A9FCCC43C98F69B97DE0A5A597660530EF38B0A546BE6D849202A8B9` and NSIS SHA-256 `3F85501BA4151B189B34F4F7A2F5D95FD92AA8F5EC8BE7FDD63238C231E179E7`. No package was installed. |
| `2026-08-24` | launch exact workspace `target/release/app.exe`; inspect current session through Windows accessibility and screenshots | passed | Exact process path distinguished the fresh build from installed Happy clients. A failed six-command group was collapsed by default with count/duration/failure metadata; expansion exposed bounded child command previews; the failed child exposed complete transcript navigation; Escape closed the transcript; half-screen layout remained bounded; dark theme rendered correctly and was restored to the original Adaptive setting. The workspace process was then closed while installed dev and official-baseline clients remained running. |
| `2026-08-24` | explicit human acceptance | passed with accepted gaps | User selected “验收并接受两个无关缺口”, accepting the packaged interaction and the named unchanged App CRLF assertion and server attachment-route failures. This authorizes `check=accepted_gaps`; it does not authorize commit, push, installation, or distribution. |

## Acceptance coverage

| Criterion | Status | Evidence |
| --- | --- | --- |
| AC2, AC3, AC4, AC6 foundations | verified | `studioToolOutputDisclosure.test.ts` and the existing transcript suite prove budgets, manual precedence, structured status, sanitization, and complete-copy separation. Mounted/layout/action acceptance remains owned by T2-T5. |
| AC1 | verified | A maximum-size successful result mounts only its one-line summary; its output sentinel is absent from the render tree. Because the body is not mounted, it is also absent from the accessibility tree. |
| AC2, AC3 | verified | Running uses a bottom-aligned 5-line clip; failure uses first 2 plus last 8 with an omission marker. Long unbroken Unicode fixtures exercise the post-wrap measurement/clip seam. Packaged narrow/zoom inspection remains T6. |
| AC4 | verified | Manual expansion and collapse remain authoritative across output and terminal-state updates. Group/scroll integration remains T4-T5. |
| AC5 | verified | Expanded transcript uses an internally scrollable `ScrollView` capped at `min(40vh, 480px)`; both the 360px and 480px branches are mounted-tested. Conversation anchoring remains T5. |
| AC11 | verified | Studio compact on/off remain bounded; Default terminal and structured Codex patch paths remain unchanged in focused tests. Whole-diff and packaged confirmation remain T6. |
| AC6 | verified | Separate command/output actions copy complete sanitized text from the disclosure model, not visible previews; ANSI/control sequences and presentation omission labels are absent. |
| AC7 | verified | The full-transcript action is present in collapsed, preview, and expanded states and opens the existing message-detail route. Studio Bash/CodexBash details render complete stored output through the shared transcript resolver. |
| AC10 | verified | Summary and actions expose concise button names; summary exposes expanded state; collapsed output is unmounted; hidden measurement text is excluded; previews are not live regions; the control remains mounted across updates. Installed RN Web Pressable source proves Enter/Space button activation; packaged keyboard/focus inspection remains T6. |
| AC8 | verified | Opening Studio terminal groups mounts child disclosure summaries without eager output; active Studio groups auto-open unless manually overridden, untouched completed groups auto-collapse, and Default remains unchanged. Completed/running/failed/pending children retain semantic disclosure; summaries include duration and nonzero failures; group collapse keeps child state mounted but hidden. Packaged evidence showed a failed six-command group collapsed by default and bounded after expansion. |
| AC9 | verified | Mounted tests prove bounded streaming height, internal end-follow ownership, native outer anchor wiring without JS scroll fights, disclosure identity, and resize state retention; packaged half-screen and disclosure interaction supplied the runtime evidence. |
| AC12 | accepted gap | Focused 64-test suite, App/server typechecks, workflow regression tests, strict audit, and diff integrity pass. Full App is 1400/1401 with an unchanged Windows CRLF assertion failure; full server is 101/102 with an unchanged attachment-route integration failure. The user explicitly accepted both gaps. |
| AC13 | verified | Fresh packaged Studio-dev runtime directly exercised collapsed/expanded failure groups, bounded child previews, full transcript navigation, Escape keyboard behavior, half-screen layout, light/dark rendering, and restoration of the original theme. Pure/mounted tests cover success, running, long-line, compact on/off, scroll anchoring, and 600px resize states that were not all simultaneously present as live history fixtures. The user explicitly accepted the packaged interaction. |

## Remaining gaps

- The user explicitly accepted the packaged interaction and the two unrelated
  full-suite gaps recorded above. They remain follow-up candidates but do not
  block this feature's finish.
- The live session supplied failure/group/light/dark/half-screen/full-transcript
  evidence. Success, running, long-line, compact on/off, exact 600px layout,
  and deterministic scroll-update cases are covered by mounted tests rather
  than separate live fixtures; this distinction is retained in AC13 evidence.
