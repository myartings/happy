# Validation: `client-long-session-performance`

Record exact commands and results. Never mark a check passed unless it ran.
Use `unavailable` when the configured tool is not installed or cannot execute,
and state the consequence under Remaining gaps; unavailable is not a pass.

| Date | Command | Result | Notes |
| --- | --- | --- | --- |
| `2026-08-28` | Read-only packaged macOS long/short Session differential | passed as planning evidence | Long Session about 101 ms median / 335 ms P95 per character versus about 30 ms / 50 ms; WebContent about 2.7 GiB resident with transient growth above 3.7 GiB; no message sent and drafts cleared. |
| `2026-08-28` | Current `quiet-forest` source and prior workflow inspection | passed as planning evidence | Confirmed existing demand-driven history, hidden-cache bounds, Session/turn projection reuse, ChatList window 9, residual `useDraft` per-value cleanup, full draft list rebuild, immediate message drain, and full message sort. |
| `2026-08-28` | `pnpm --filter happy-app exec vitest run sources/features/client-performance/clientLongSessionDiagnostics.test.ts` | unavailable | This worktree has no local executable links, so pnpm returned `Command "vitest" not found`; the repository-owned v3.2.4 binary was used directly below without installing dependencies. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/features/client-performance/clientLongSessionDiagnostics.test.ts` from `packages/happy-app` | passed | 1 file, 4 tests; explicit-disabled capture plus the 100/500/2,000 Session and 100/1,000/5,000 message matrix passed. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/hooks/useDraft.test.ts` from `packages/happy-app` | expected RED | 1 file, 1 intended failure. Before the debounce expires, current code writes `['session-1', 'a']` and the intermediate `['session-1', 'ab']`; expected only the initial empty-to-non-empty write. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/tsc --noEmit` from `packages/happy-app` | passed | Used a temporary ignored workspace link from `@slopus/happy-wire` to the same-HEAD main-worktree build artifact; the link was removed immediately after the check. |
| `2026-08-28` | `git diff --check` | passed | No whitespace errors in the T1 diff. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/hooks/useDraft.test.ts` during T2 tracer bullets | expected REDs, then GREEN | Seven intended REDs proved: per-value cleanup saved `ab`; Session switch wrote new B text into A; clear resurrected `ab`; background duplicated `ab`; hydrate rewrote `""`; rapid A→B→A duplicated saved A; and the real draft-write counter remained zero. Each received the smallest corresponding GREEN before the next RED. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/hooks/useDraft.test.ts sources/features/client-performance/clientLongSessionDiagnostics.test.ts` from `packages/happy-app` | passed | 2 files, 15 tests. Covers rerender cleanup, 100-character budget, diagnostics count, background/inactive, true unmount, Session switch/A→B→A, hydrate, clear/AppState/timer race, stable listener, and persistence retry. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/hooks` from `packages/happy-app` | passed | 7 files, 73 tests; all hook-adjacent suites passed. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/tsc --noEmit` from `packages/happy-app` after T2 | passed | Used the same temporary ignored same-HEAD `@slopus/happy-wire` link required by this dependency-light worktree; trap cleanup removed it after the check. |
| `2026-08-28` | Workflow validation, strict active audit, tracked and untracked `git diff --check` after T2 | passed with expected future gaps | Workflow adoption and state validation passed; strict audit reports only pending T3–T7 implementation/check/review/finish gates; no whitespace errors. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/sync/storage.sessionDraft.test.ts` during T3 tracer bullets | expected REDs, then GREEN | Five intended REDs proved the old action replaced Zustand state and wrote MMKV for an unchanged value, reconstructed persistence from hydrated Sessions and dropped an unhydrated draft, replaced unaffected project containers, reloaded `session-drafts` during deletion, and left real storage counters at zero. Each received the smallest corresponding GREEN before the next RED. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/sync/storage.sessionDraft.test.ts` from `packages/happy-app` | passed | 1 file, 10 tests. Covers true normalized no-op, write-failure atomicity and retry, whitespace clearing and repeated-clear no-op, authoritative snapshot preservation, 100/500/2,000 Session identity/order matrices, archived direct-row patching, deletion without reload, and exact read/write/reprojection counters. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/sync/projectTodos.test.ts sources/features/github-issues/githubIssuesDetailScreen.test.ts sources/features/github-issues/githubIssuesWorkspace.test.ts sources/features/client-performance/sessionRowProjectionCache.test.ts sources/features/client-performance/clientLongSessionDiagnostics.test.ts sources/hooks/useVisibleSessionListViewData.test.ts` from `packages/happy-app` | passed | 6 files, 43 tests; project-todo and GitHub-issue draft insertion callers plus adjacent Session-list projection behavior remain green. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/tsc --noEmit` from `packages/happy-app` after T3 | passed | Used the temporary ignored same-HEAD `@slopus/happy-wire` workspace link; trap cleanup removed it after the check. |
| `2026-08-28` | `python3 scripts/workflow-check.py --only check` | passed with expected future gaps | All four configured workflow checks ran with zero failures: adoption validation, 14 workflow-core tests, 14 workflow-CI tests, and strict audit. Audit reports only the still-pending overall implementation/check/review/finish gates for T4–T7; the overall check gate was intentionally not recorded during this T3 slice. |
| `2026-08-28` | Combined T2/T3 focused Vitest gate from `packages/happy-app` | passed | 9 files, 70 tests. The expected stderr belongs to the existing malformed pending-settings JSON recovery test; draft persistence compatibility retains the `session-drafts` key and flat JSON record with CJK and emoji values. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/sync/sync.realtimeRecovery.test.ts` before the temporary workspace link | unavailable | The dependency-light worktree could not resolve `@slopus/happy-wire`, so Vitest collected no tests. This setup failure is not counted as a RED; all meaningful runs below used the temporary same-repository workspace link and removed it by trap. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/sync/sync.realtimeRecovery.test.ts` during the first T4 tracer bullet | expected RED, then GREEN | The real socket→decrypt→storage path exposed all 100 same-window messages before 24 ms (`expected length 0, received 100`). After the scheduler integration, the same reconnect burst stays invisible through 23 ms and publishes all 100 messages once at 24 ms with `messageQueueBatches=1`. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/features/client-performance/sessionMessageBatchScheduler.test.ts` during T4 tracer bullets | two expected REDs, then GREEN | Timer creation failure first threw instead of using the immediate fallback; an in-flight old generation then allowed a second Session owner to start. Both REDs received minimal fixes. The final 9 tests cover 24 ms/FIFO, per-Session isolation, arrivals during drain, generation replacement, cancel/in-flight invalidation, explicit flush, shutdown, 32 ms cap, and scheduler-failure fallback. |
| `2026-08-28` | Focused T4 scheduler/realtime/cache gate | passed | 4 files, 24 tests: scheduler 9, real Sync/reconnect 4, realtime recovery 4, and message-cache policy 7. Public integration proves waiting occurs outside `AsyncLock`, deletion cancels a pending timer without stale apply, and the reconnect burst is neither lost nor duplicated. |
| `2026-08-28` | Combined T1–T4 focused Vitest gate from `packages/happy-app` | passed | 13 files, 94 tests. Draft lifecycle/storage, diagnostics, Session projections, caller integrations, per-Session batching, realtime recovery, and cache policy all remain green. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/tsc --noEmit` from `packages/happy-app` after T4 | passed | Used the temporary ignored same-HEAD `@slopus/happy-wire` workspace link; trap cleanup removed it after the check. |
| `2026-08-28` | `python3 scripts/workflow-check.py --only check` after T4 | passed with expected future gaps | All four configured workflow checks ran with zero failures: adoption validation, 14 workflow-core tests, 14 workflow-CI tests, and strict audit. The overall check gate remains intentionally pending while T5–T7 are unimplemented. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/vitest run sources/features/client-performance/orderedMessageCollection.test.ts` before T5 implementation | expected RED, then GREEN | The public 5,000-message storage fixture proved one normal tool-result replacement called `Array.sort` once (`expected not called, received 1`). After the ordered collection was wired through every storage message-update entry, the replacement performs no sort, retains all 4,999 unaffected `Message` identities, and records zero fallbacks. |
| `2026-08-28` | Focused ordered collection and storage integration suite | passed | 1 file, 9 tests. Covers stable replacement, newest insert, older append, duplicate replay identity, mixed/equal-timestamp order, repeated update-ID dedupe, 100 deterministic mixed batches equivalent to the legacy stable full sort, permission updates through `applySessions`, and corrupted-order fallback with one development warning/counter and no prior-array mutation. |
| `2026-08-28` | Combined T1–T5 and message-equivalence Vitest gate from `packages/happy-app` | passed | 20 files, 199 tests. Draft lifecycle/storage, diagnostics, Session projections, message batching/cache/recovery, ordered collection, reducers, permission handling, sidechains, grouping, turn projections, and baseline fixtures all remain green. The expected stderr belongs to the existing malformed pending-settings JSON recovery test. |
| `2026-08-28` | `/Users/myartings/workspace/happy/node_modules/.bin/tsc --noEmit` from `packages/happy-app` after T5 | passed | Used the temporary ignored same-HEAD `@slopus/happy-wire` workspace link; trap cleanup removed it after the check. |
| `2026-08-28` | `python3 scripts/workflow-check.py --only check` after T5 | passed with expected future gaps | Adoption validation, 14 workflow-core tests, 14 workflow-CI tests, and strict audit all completed with zero command failures. The overall implementation/check/review/finish gates remain intentionally pending for T6–T7. |
| `2026-08-28` | T6 policy, staging, atomic-storage, and commit-race tracer bullets | expected REDs, then GREEN | REDs proved the absent protection matrix/boundary selector, absent staged reducer/ID remap, absent one-publication store swap, and absent generation/seq/session/UI commit gate. Final focused helpers cover strict 750-message/20 MiB triggers, 500-message/10 MiB targets, complete user/tool boundaries, every protected state, optimistic `localId` joins, content mismatch, multi-view UI aggregation, and all commit-race reasons. |
| `2026-08-28` | Mounted ChatList and real Sync visible-tail integration | passed | Mounted ChatList reports live-tail, older-reading, target, viewport-settle, and source cleanup states. Real Sync tests prove one 760→500 publication with newest IDs/content unchanged, cursor already 261 inside the publication callback, `hasMoreOlder=true`, successful `before_seq=261` reload to 600 messages, seq-race abort with the exact old entry reference, and fetch/decrypt failures with zero swap. |
| `2026-08-28` | AC9–AC13 focused regression family | passed | 14 files, 72 tests before the final failure cases; policy/staging/storage/diagnostics, cache bounds, ordered collection, grouping, copy, prompt navigation, target resolution, mounted ChatList, and Sync recovery all passed. |
| `2026-08-28` | First combined T1–T6 command using a temporary workspace link | partial setup failure | 28 files and 171 tests passed, but `reducer.spec.ts` did not collect because the temporary link path was mistakenly relative to the package workdir twice. The empty nested directories were removed immediately; no source changed. The corrected complete command below supersedes this setup failure. |
| `2026-08-28` | Corrected combined T1–T6 Vitest gate plus `/Users/myartings/workspace/happy/node_modules/.bin/tsc --noEmit` from `packages/happy-app` | passed | 29 files, 234 tests and Happy App TypeScript passed. The ignored same-HEAD `@slopus/happy-wire` link was created only for the command and removed by trap; no dependency install occurred. |
| `2026-08-28` | `python3 scripts/workflow-check.py --only check` after T6 | passed with expected future gaps | Adoption validation, 14 workflow-core tests, 14 workflow-CI tests, and strict audit completed with zero command failures. Audit reports only the intentionally pending overall implementation/check/review/finish gates owned by T7. |
| `2026-08-28` | Temporary-link cleanup and `git diff --check` after T6 | passed | The ignored workspace link is absent and no whitespace errors were found. |
| `2026-08-28` | `pnpm --filter happy-app exec vitest run` | passed with unrelated baseline gaps pending acceptance | Complete collection ran 191 files and 1,631 tests: 187 files/1,616 tests passed; 4 files/15 tests failed. None of the failing tests or their production subjects is modified by this workflow. `ToolViewStudioPresentation.test.ts` has 13 pre-existing Studio margin expectations (`1`, current `2`); `StudioMarkdownOptions.test.ts` has one pre-existing non-Studio accessibility-role expectation; `studioRichTextWiring.test.ts` has one stale source-string expectation; `flatSessionListPreferenceWiring.test.ts` references the absent `sources/app/(app)/settings/features.tsx`. User acceptance is required by AC14. |
| `2026-08-28` | Focused rerun of the four full-suite baseline failures | reproduced unchanged | 4 files failed, 15 failed/24 passed. The exact assertions and missing-file collection error match the complete-suite run, confirming a stable named baseline rather than a failure in the T2-T7 files. |
| `2026-08-28` | `pnpm --filter happy-app typecheck` and `pnpm --filter happy-server typecheck` | passed | Both exact package scripts passed after temporary ignored links exposed the repository-owned `tsc` binary and this worktree's same-source `@slopus/happy-wire`; all links were removed by trap and no dependency install occurred. The first exact attempt without the executable link failed only with `sh: tsc: command not found` and is superseded by this run. |
| `2026-08-28` | `pnpm --filter happy-app tauri:build:dev` | unavailable as configured | Web export and Rust compilation succeeded after building this worktree's ignored `happy-wire/dist`, but the configured bundle step requires the unavailable local identity `Developer ID Application: Bulka, LLC (466DQWDR8C)`. This is a machine signing prerequisite, not a source compile failure. |
| `2026-08-28` | `pnpm --filter happy-app exec tauri build --config src-tauri/tauri.dev.conf.json --bundles app --no-sign` | passed | Built exactly one local validation bundle at `packages/happy-app/src-tauri/target/release/bundle/macos/Happy (dev).app` without installation or replacement. Identifier `com.slopus.happy.dev`, version `0.1.0`, arm64 executable, bundle about 26 MiB, executable SHA-256 `895ae821e66280145885cb371e0a87213aa87a5a97495d8fab8cc6a1d6728689`. The bundle is intentionally unsigned/unsealed and is not a distribution artifact. |
| `2026-08-28` | Workspace-bundle launch and process-identity validation | passed | Launched the exact workspace executable in separate validation instances without replacing or terminating `/Applications/Happy (dev).app` PID 70203. Final isolated automation copy PID 83421 had associated WebContent PID 83424; its executable hash matched the workspace binary. Every validation-only instance was closed afterward. |
| `2026-08-28` | Ten one-second idle samples from validation PIDs 83421/83424 | passed as bounded startup evidence | App RSS remained 94,896 KiB for all ten samples; associated WebContent RSS settled from 131,904 to 131,888 KiB; both sampled at 0.0% CPU. This proves clean startup and idle stability only, not the long/short typing or rebase memory budget. |
| `2026-08-28` | Packaged typing, streaming/scroll smoke, and three eligible tail-rebase cycles | unavailable pending user acceptance or manual cooperation | Computer Use could not bind the Tauri window (`cgWindowNotFound`) even after a hash-identical, uniquely named copy was launched. Metadata window inventory resolved exact PIDs/windows, but lossless capture failed with `could not create image from window`, so the required safe interaction path stopped at the permission boundary. No message was sent, no draft was created or changed, and no alternate UI automation was used. AC15 latency and three-cycle memory budgets therefore remain unproven. |
| `2026-08-28` | Validation-process cleanup | passed | Closed only validation PIDs 82652, 83038, and 83421 plus their children; the installed app PID 70203 remained running. The generated uniquely named automation copy was moved to `~/.Trash/Happy T7 Validation.app` and is recoverable; the workspace build remains in the ignored Tauri target. |
| `2026-08-28` | Whole-diff correctness/data-integrity/concurrency/memory/scroll/rollback review | passed | Reviewed all modified and untracked product, test, spec, task, and workflow files. No unresolved blocking, high, or medium finding remains. The review specifically covered draft lifecycle isolation and failure retry, persistence atomicity, scheduler ownership/generation races, ordered-collection identity/order fallback, tail staging/commit rollback, cursor/pagination restoration, multi-view UI fail-closed behavior, and independent rollback seams. |
| `2026-08-28` | Post-review affected regression gate | passed | 17 files, 83 tests. Covers every new client-performance helper plus public draft storage, hook lifecycle, realtime Sync, ChatList scroll/signal, and persistence seams. The expected malformed-JSON recovery stderr remains green behavior. |
| `2026-08-28` | `python3 scripts/validate-happy-workflow.py`; `python3 scripts/test-workflow-core.py`; `python3 scripts/test-workflow-ci.py`; `python3 scripts/workflow-audit.py --strict --require-active client-long-session-performance` | passed with pending acceptance gates | Adoption validation passed; workflow-core 14/14 and workflow-CI 14/14 passed; strict audit reports only check/review/finish at the time of the run. The review gate was then recorded from the whole-diff receipt; check and finish intentionally remain open for explicit user acceptance. |
| `2026-08-28` | Tracked and untracked whitespace validation | passed | `git diff --check` reported no tracked whitespace error; per-file `git diff --no-index --check` reported no untracked whitespace error. |

## Acceptance coverage

| Criterion | Status | Evidence |
| --- | --- | --- |
| AC1–AC2 draft lifecycle | verified | 11 real hook tests prove value rerenders do not flush, a 100-character burst writes exactly initial/trailing values, background/inactive/true unmount/Session switch flush exact latest values once, A→B→A remains isolated, hydrate does not rewrite, clear prevents resurrection, AppState stays singly subscribed, and failures remain retryable. |
| AC3–AC4 draft storage/projection | verified | Public storage tests prove an unchanged normalized draft returns the exact state with zero MMKV writes or subscriber notification; one changed draft preserves unrelated Session/row/container identities and order at 100/500/2,000 Sessions while reprojecting at most one row. A failed write advances neither Zustand nor the module snapshot and remains retryable. The snapshot preserves unhydrated drafts, deletion updates it without reload, and compatibility tests preserve the existing key/flat JSON shape. |
| AC5–AC6 message scheduling/lifecycle | verified | A real reconnect burst of 100 socket updates forms one FIFO scheduler batch and one Session-message publication at exactly the configured 24 ms boundary, with no visibility at 23 ms. Pure and integration tests prove one owner per Session, independent Session timers, arrivals during drain, waiting outside `AsyncLock`, explicit flush/shutdown, timer-failure fallback, generation invalidation, and deletion without stale apply. |
| AC7–AC8 message ordering/collection | verified | Public storage and pure collection tests prove stable existing replacements, newest/middle/equal-time inserts, older-page appends, replay dedupe, and deterministic descending order across 100 mixed batches. A 5,000-message tool-result update performs zero full sorts/fallbacks and preserves all 4,999 unaffected identities; detected invariant corruption alone rebuilds, warns in development, and increments `messageSortFallbacks`. |
| AC9–AC13 visible-tail/rebases | verified | Pure and mounted tests prove count/byte hysteresis, every busy/reading/target/composition protection, complete user/tool boundary extension, multi-view fail-closed aggregation, staged reducer equivalence with stable IDs and optimistic `localId`, one atomic store/cursor publication, generation/seq/session/UI race rejection, fetch/decrypt rollback, and discarded-head reload through `before_seq`. Existing cache, reducer, grouping, copy, prompt-target, and scroll-anchor suites remain green. |
| AC14 complete applicable validation | accepted gap | App/server typechecks and all 234 focused implementation tests pass. The complete Happy App suite collected successfully but has 4 unrelated baseline files/15 failures named above; the user explicitly accepted this gap on `2026-08-28`. |
| AC15 packaged runtime budgets | accepted gap | The exact no-sign workspace bundle builds, launches, idles stably, and leaves the installed app untouched. Safe automation/capture could not bind the Tauri window, so long/short input P95, streaming/scroll smoke, and three rebase-cycle RSS evidence were not collected. The user explicitly accepted this limitation on `2026-08-28`; it is not represented as a performance-budget pass. |
| AC16 whole-diff review | verified | Whole-diff review found no unresolved blocking/high/medium issue, and the post-review affected regression gate passed 17 files/83 tests. |

## Remaining gaps

- T1–T7 product implementation and review are complete. The check gate records
  the user's explicit acceptance of the two named gaps; finish/archive evidence
  owns the remaining workflow closure.
- Draft Session-read, successful persistence-write, and row-reprojection
  counters plus successful message-queue batch counts now live at their actual
  boundaries and remain allocation-free while no explicit capture is active.
  Message-sort fallback and retained-tail/rebase diagnostics now live at their
  actual invariant boundaries, including abort reasons and boundary excess.
- The complete Happy App suite has four named, unrelated baseline files with
  15 failures. They are not modified by this workflow, and the user accepted
  the gap on `2026-08-28`.
- Exact packaged input latency, streaming/scroll behavior, and three-cycle RSS
  reduction remain unavailable because the approved desktop interaction and
  capture paths could not bind/capture the Tauri validation window. Startup and
  idle process identity are proven; AC15 performance budgets are not, and the
  user explicitly accepted that evidence gap on `2026-08-28`.
- Deterministic retained count/byte and operation counters remain the automated
  regression gates, but they do not substitute for the explicitly required
  packaged interaction acceptance.
