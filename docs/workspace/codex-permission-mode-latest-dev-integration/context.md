# Context: `codex-permission-mode-latest-dev-integration`

This is a serial merge-local integration in the current Issue #87 worktree.
The only conflicts are `docs/workspace/archive.md` and
`packages/happy-cli/src/api/types.ts`. The authoritative parents are current
HEAD `4bb475d8` and `origin/dev@03936270`; no implementation work is dispatched.

Relevant contracts are the Issue #87 permission preservation spec, the dev
runtime-confirmed route spec, their focused tests, and repository workflow
merge rules. Verification review may use independent read-only contexts.
