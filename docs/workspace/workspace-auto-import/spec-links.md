# Specification Links: `workspace-auto-import`

- PRD: not required — accepted local follow-up request
- Feature spec: `docs/specs/workspace-auto-import.md`
- Architecture: existing `SavedProjectRegistry` and `workspaceProjectScanner`
- ADRs: none; schema and external protocol remain unchanged
