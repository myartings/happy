# Decisions: `codex-first-happy-client-latest-dev-refresh`

| ID | Question | Status | Decision/evidence |
| --- | --- | --- | --- |
| D1 | How is the newly advanced `dev` integrated without invalidating the completed pinned merge? | decided | Create a second ordinary merge commit with first parent `ddb3034e2e3006b9b70f1e38d6cced99cdef1de0` and second parent `87b5385e82d96b5eaab68bc65a968cf36167e9c5`. This preserves both audited merge boundaries and obeys the no-rewrite rule. |
| D2 | How is `archive.md` resolved? | decided | Preserve the exact row union from both parents: both Codex-first rows plus `needs-attention-current-requests`, each exactly once, followed later by this workflow's canonical terminal row. |
| D3 | How are the `SessionView` and `SessionsList` conflicts resolved? | decided | Keep both import sets. The prospective body already retains Codex-first runtime/layout/navigation calls and current-request projection/focus calls; no new component behavior is invented. |
| D4 | Which list-projection semantics win? | decided | Preserve current `dev` attention metadata, one-row deduplication, offline visibility, severity ordering, stable tie-break, and setting-off behavior. Also preserve the Codex-first parent fallback that a legacy `input_required` row without attention metadata is answer-level actionable. When attention metadata exists it is authoritative, so stale legacy state cannot override its reason. Pinning never reorders attention severity. |
| D5 | What is the TDD sequence? | decided | Resolve initially to current-`dev` list semantics while retaining both parents' tests. The existing Codex-first `promotes Agent input requests` test must fail for the intended missing fallback (RED). Add only the minimal fallback/priority behavior, then run that test, the complete visible-list file, PR #76's focused family, and the App suite (GREEN and regression). |
| D6 | What validation is required? | decided | Run focused current-request/navigation/list/Codex-first seams, App and Server typechecks/suites through the applicable candidate check, workflow validation/CI, source scans, Windows doctor/smoke and an uninstalled native build, then independent whole-candidate review. Parent-reproduced failures may be accepted only through exact candidate-bound evidence. |
| D7 | Which external mutations remain authorized? | decided | After finish/archive and committed CI, normally push this feature branch and update/verify PR #78. Do not merge or close the PR, install/replace a client, sign, publish, release, delete branches, or force push. |
