# Studio Desktop Visual Tasks

## Plan

- [x] Capture a lossless current Codex Desktop light reference.
- [x] Extract a validated visual-evidence record.
- [x] Inspect Happy theme, typography, appearance settings, and core components.
- [x] Define the low-change visual theme contract.
- [x] Compare macOS and Windows platform chrome.
- [x] Establish a recurring Windows popover family from three runtime examples.
- [x] Rewrite the contract around explicit surface and elevation levels.
- [x] Review and accept the original Codex supporting-evidence specification.
- [x] Accept the reset Studio contract: neutral name,
  packaged-desktop-only boundary, and mandatory human visual gates.
- [x] Extract and validate a bounded Otty reference state as supporting evidence.
- [x] Switch the primary reference to Codex after user review.
- [x] Extract and validate the Codex light main-window design system.
- [x] Produce and obtain user acceptance for the first Happy main-window Pencil design.
- [x] Produce and obtain user acceptance for a v2 Pencil design that preserves
  Happy's functional map while boldly adopting Codex component geometry.
- [x] Propose exactly one implementation slice from the accepted Pencil design.
- [x] After user authorization, implement only that accepted item.
- [x] Build the packaged desktop client and obtain explicit user acceptance of
  the visible result before proposing another item.
- [ ] Repeat the single-item loop; never batch pending visual improvements.
- [ ] Eventually capture missing dark and interactive reference states.

## Verify

- The specification preserves Happy functionality and macro layout.
- Every exact visual claim is sourced from runtime or static evidence.
- Screenshot-derived estimates are labeled as estimates.
- Proposed interaction and shadow values are not presented as observed facts.
- The implementation plan is token-first and keeps `packages/codium` decoupled.
- Persistent regions, contained content, composer, popovers, and modals have
  distinct and testable hierarchy rules.
- Popovers share one shell, use no visible scrim, and preserve adaptive placement.
- Every product-code slice has proposal approval and result acceptance as two
  separate human gates.
- Standalone Web, iOS, and Android remain Default and expose no Studio selector.

## Progress

- 2026-08-10: Light reference and evidence record completed.
- 2026-08-10: Happy theme architecture and core component styles inspected.
- 2026-08-10: Initial visual theme specification authored.
- 2026-08-10: Cross-platform and popover-family evidence incorporated; the
  specification was rewritten around a seven-level hierarchy model.
- 2026-08-12: Rejected the batch-style Studio implementation and reset the
  contract to packaged-desktop-only, single-item human acceptance.
- 2026-08-12: User changed the primary visual reference from Otty to Codex after
  reviewing both directions; Codex main-window extraction passed validation.
- 2026-08-12: Generated `studio-main-window-v1.pen/png`; user visual acceptance
  passed explicitly before any product-code proposal.
- 2026-08-12: User clarified that functional layout must remain stable while
  component geometry and regional proportions may be redesigned boldly; v2
  Pencil design passed explicit user acceptance before any implementation.
- 2026-08-12: Slice v2-01 introduced the desktop-only Studio activation seam,
  responsive sidebar frame, near-white Region, white Canvas, and one outer
  divider without changing session-row internals. The rebuilt local desktop
  result received explicit user acceptance before any second slice.
- 2026-08-12: Slice v2-02 replaced desktop Studio session cards with 62 pt rich
  rows and a fill-only selected state while preserving all existing metadata
  and behavior. The installed result and selected state received explicit user
  acceptance before any third slice.
- 2026-08-13: Slice v2-03 compacted only the desktop Studio New Session/archive
  group to the accepted 38 pt geometry while preserving Todo and all behavior.
  The installed result received explicit user acceptance before any fourth slice.
- 2026-08-13: Slice v2-04 tightened only the packaged-desktop Studio Todo
  utility row's internal padding and content gap while preserving its existing
  36 pt height, 10 pt radius, hairline, outer inset, and all behavior. The
  installed result received explicit user acceptance before any fifth slice.
- 2026-08-13: Slice v2-05 compacted only the packaged-desktop Studio first-level
  session-list headings to a quieter 12/16 pt hierarchy with tighter container
  padding. Existing strings, list behavior, nested headings, and adjacent UI
  remain unchanged. The installed result received explicit user acceptance
  before any sixth slice.
- 2026-08-13: Slice v2-06 tightened only the packaged-desktop Studio session
  status, title, and metadata alignment by using a 10 pt status slot, 6 pt gap,
  and 16 pt metadata inset. Row geometry, content, status semantics, and
  interactions remain unchanged. The installed result received explicit user
  acceptance before any seventh slice.
- 2026-08-13: Slice v2-07 removed only the packaged-desktop Studio residual
  full-width active-period group boundary, leaving the existing headings and
  whitespace to carry hierarchy. Session rows, period spacing, ordering, and
  interactions remain unchanged. The installed result received explicit user
  acceptance before any eighth slice.

## Finish

The reset contract is a documentation-only proposal. No product implementation
is authorized until the user accepts it and separately approves one bounded
visual improvement.
