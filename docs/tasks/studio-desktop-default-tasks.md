# Studio Desktop Default Tasks

- [x] Add failing policy, compatibility-default, and build-command tests.
- [x] Force Studio at the central packaged-Tauri visual-style resolver.
- [x] Make Studio the device-local compatibility default without removing the
  persisted enum.
- [x] Embed Studio explicitly in the Tauri production export with `cross-env`.
- [x] Run focused tests, typecheck, full applicable tests, workflow checks, and
  whole-diff review.
- [x] Build and launch a fresh personal Tauri bundle without overwriting the
  installed app; verify the runtime no longer honors a stale Default visual-style
  value.
- [x] Archive the workflow and prepare the validated change for integration.
