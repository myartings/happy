# Studio Sidebar Unboxed Groups Tasks

## Batch 0 — accepted contract and isolated ownership

- [x] Record the user-approved unboxed-group contract and Studio-only boundary.
- [x] Limit this writer to `feature/studio-sidebar` in the isolated
  `codex-visual-theme` worktree; block conversation, composer, semantic,
  overlay, and shared integration workflow files.

## Batch 1 — presentation policy and implementation

- [x] Add a pure group-presentation decision with focused Studio, Default, and
  non-Tauri tests.
- [x] Route active-session groups through the unboxed Studio container without
  inheriting default card chrome.
- [x] Route project/workspace groups and project headers through the same
  presentation decision without changing controls or row structure.

## Batch 2 — verification and handoff

- [x] Run focused tests, Happy App typecheck, workflow checks, and whole-diff
  review.
- [x] Prepare the archived atomic scope for one local parent-owned cherry-pick
  commit.
- [ ] Parent integrates, builds, captures, and returns the screenshot for
  explicit user visual acceptance.
