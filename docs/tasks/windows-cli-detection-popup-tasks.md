# Windows CLI detection popup tasks

- [x] Add Windows regression tests for direct PowerShell and Antigravity PATH
  detection with `windowsHide: true`.
- [x] Replace both Windows string-form `execSync` paths with direct executables.
- [x] Run focused tests, typecheck, and the complete Happy CLI unit suite.
- [x] Build/install the local CLI, restart the daemon, and capture foreground
  windows for at least two minutes.
- [x] Confirm the SSH firewall restrictions remain `LocalSubnet`.
