interface:
  display_name: "Generate Tasks"
  short_description: "Create local fresh-Session Task plans"
  default_prompt: "Use $generate-tasks to propose and create self-contained local Task plans for accepted fresh-Session slices."
