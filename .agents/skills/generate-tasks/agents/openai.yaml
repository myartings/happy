interface:
  display_name: "Generate Tasks"
  short_description: "Create linked Task and Issue pairs"
  default_prompt: "Use $generate-tasks to propose fresh-Session slices and, after approval, create linked Task File and GitHub Issue pairs."
