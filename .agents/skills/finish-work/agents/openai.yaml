interface:
  display_name: "Finish Task Work"
  short_description: "Complete Task metadata and reusable guidance"
  default_prompt: "Use $finish-work after final checks and code-review to complete the Task before the same local commit."
