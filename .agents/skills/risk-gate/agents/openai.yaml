interface:
  display_name: "Risk Gate"
  short_description: "Define controls for consequential changes"
  default_prompt: "Use $risk-gate to add proportionate controls to the existing work contract."
